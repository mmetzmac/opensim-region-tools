# coordinates.txt should contain all coordinates with it's reagion
# ports.txt should contain all ports with it's regions
# land.txt should contain all regionnames in lowercases, which are enabled for the os script
